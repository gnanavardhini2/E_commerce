# Backend uses Render-provided PORT in production, defaults to 8080 locally
spring.application.name=backend
server.port=${PORT:8080}
# PostgreSQL DB config

spring.datasource.url=jdbc:postgresql://ep-blue-math-a1cr7io4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_BH7DtIi1Xelm

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.naming.physical=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
spring.jpa.properties.hibernate.physical_naming_strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

# JWT secret (should be overridden in production)
JWT_SECRET=wlmdtuaprzfchhiv1234567890abcdef9876543210abcd

# Email config
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:gnanavardhini2@gmail.com}
spring.mail.password=${MAIL_APP_PASSWORD:walx edua dzmj gwxc}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${MAIL_SMTP_STARTTLS_REQUIRED:true}
spring.mail.properties.mail.smtp.connectiontimeout=${MAIL_SMTP_CONNECTION_TIMEOUT:30000}
spring.mail.properties.mail.smtp.timeout=${MAIL_SMTP_TIMEOUT:30000}
spring.mail.properties.mail.smtp.writetimeout=${MAIL_SMTP_WRITE_TIMEOUT:30000}

# Optional: admin copy for all outgoing order emails
mail.admin.bcc=${MAIL_ADMIN_BCC:}

# Frontend base URL used in email links
app.frontend.base-url=${APP_FRONTEND_BASE_URL:http://localhost:3000}

# Backend base URL used to generate absolute links for uploaded images
app.backend.base-url=${APP_BACKEND_BASE_URL:http://localhost:8080}

# Multipart upload limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
